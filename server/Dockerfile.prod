FROM python:3.12-slim

COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/

WORKDIR /app

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev

COPY . .

# start.sh に実行権限を付与
RUN chmod +x start.sh

# 起動スクリプト経由で起動（マイグレーション → サーバー起動）
CMD ["./start.sh"]
